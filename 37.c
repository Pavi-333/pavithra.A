#include<stdio.h>
void main()
{
int p,m,temp;
printf("Enter the two numbers:");
scanf("%d%d",&a,&b);
printf("Before swapping a=%d b=%d",a,b);
temp=p;
p=m;
m=temp;
printf("\n After swapping %d %d",a,b);
return 0;
}
